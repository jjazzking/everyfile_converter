# everyfile-converter

회계 실무에서 오가는 파일 포맷 사이의 변환을 한 곳에서 처리하기 위한 도구입니다.
**변환 엔진 + 미리보기 대시보드**가 동작하며, PDF 표 추출이 다음 단계입니다.

같은 빌드가 두 곳에서 돕니다.

| | 어디서 도는가 | 파일이 나가는가 |
|---|---|---|
| **브라우저 실행** (GitHub Pages) | 보는 사람의 브라우저 안 | 나가지 않음 |
| **서버 실행** (`everyfile serve`) | 사내 서버 | 사내 서버까지 |

화면은 어느 쪽인지 모릅니다 — 실행 시점에 `/api/health` 응답 여부로 고릅니다.
`?engine=browser` 로 강제할 수 있습니다.

```bash
uv venv && uv pip install -e ".[dev]"
.venv/bin/everyfile serve          # http://127.0.0.1:8000
```

첫 화면은 예시 장부가 실제로 변환된 상태로 열립니다. 컬럼 헤더의 **타입 배지**를 누르면
좌우 그리드가 즉시 다시 계산됩니다.

## 왜 이런 구조인가

### 1. N×M 이 아니라 중간표현(IR)

포맷이 5개면 조합은 20개가 되고, HWP 하나만 추가해도 30개가 됩니다. 모든 변환이
공통 IR 을 거치므로 **파서 N개 + 라이터 N개**로 끝납니다.

```
파일 ──readers──▶ Document/TableIR ──convert──▶ ConversionResult ──writers──▶ 파일
                                          └────preview────▶ 화면 페이로드
```

### 2. 모든 행이 원본 위치를 끝까지 들고 다닌다

`SourceRow.index` 는 원본 파일의 행 번호입니다. 소계 행이 제외되거나 배열이 평탄화되어
행 수가 달라져도 변환 결과가 원본 어느 줄에서 왔는지 잃지 않습니다. 미리보기 화면의
좌우 패널 정렬, 이슈 보고, 검수 추적이 전부 이 앵커 위에 서 있습니다. 나중에 붙이려면
IR 을 다시 설계해야 하므로 처음부터 넣었습니다.

### 3. 화면은 실행 방식을 모른다

화면에는 `fetch` 가 없습니다. `web/backend.js` 의 인터페이스(`open` / `preview` /
`convert` / `jsonSchema`)만 부르고, 그 뒤에 두 구현이 꽂혀 있습니다.

```
화면 ─▶ Backend 인터페이스 ─┬─▶ PyodideBackend  (웹 워커 · 같은 파이썬)
                            └─▶ HttpBackend     (사내 서버 FastAPI)
```

브라우저와 서버가 **같은 파이썬 코드**(`everyfile.session.Session`)를 탑니다.
두 실행 환경이 다른 코드를 타면 "브라우저에선 되는데 서버에선 값이 다르다" 가
반드시 생깁니다.

이 층이 없으면 실행 방식을 바꿀 때 화면을 통째로 다시 만들어야 합니다.
`tests/test_api.py::test_page_does_not_talk_to_the_api_directly` 가 이 불변조건을 지킵니다.

### 4. 변환은 값이 아니라 "값 + 근거"를 돌려준다

셀 하나를 변환하면 결과값과 함께 **규칙 체인**과 **이슈**가 남습니다.

```
(240,000)  →  -240000    괄호→음수 › strip:thousands › cast:number
```

회계 자료는 숫자 하나가 틀리면 끝나기 때문에, "왜 이 값이 되었는가" 를 화면에서 셀 단위로
확인할 수 있어야 합니다.

### 5. 값을 조용히 잃지 않는다

- **계정코드의 선행 0**: `CODE` 타입은 문자열로 보존하며, 숫자로 캐스팅하면 경고를 남깁니다.
  엑셀 출력에서도 셀 서식이 아니라 셀 타입 자체를 텍스트로 씁니다 (서식만으로는 지켜지지 않음).
- **변환 실패**: 값을 버리고 `null` 로 만들지 않고 원문을 유지한 채 ERROR 로 보고합니다.
- **단위 표기**: `(단위: 천원)` 을 발견하면 알리기만 하고 자동으로 곱하지 않습니다.
  잘못 곱하면 조용히 1000배 틀린 값이 나갑니다.
- **`-` 표기**: 0이 아니라 '해당 없음'으로 읽습니다.

## 대시보드

`everyfile serve` 로 띄웁니다. 화면이 하는 일:

- **좌우 그리드** — 원본과 결과가 원본 행 번호로 짝지어져 스크롤이 동기화됩니다.
  출력에서 빠진 행(소계 등)은 빗금 gap 으로 자리를 지킵니다.
- **타입 배지** — 누르면 프로파일이 바뀌고 서버가 표본을 다시 변환합니다.
  `계정코드` 를 `정수` 로 바꾸면 선행 0 이 사라진다는 경고가 즉시 올라옵니다.
- **셀 클릭** — 하단 상태바에 원본값 / 변환값 / 적용된 규칙 체인이 표시됩니다.
- **JSON 뷰** — 결과를 객체 배열로 보며, 행 선택이 그대로 이어집니다.
- **드래그&드롭 업로드**, 시트 선택, 변환 결과 내려받기, JSON Schema 내보내기.

화면은 프로파일을 통째로 들고 있다가 매 요청에 실어 보냅니다. 서버가 세션 상태로
프로파일을 들고 있으면 여러 탭에서 같은 파일을 다른 규칙으로 여는 순간 서로를
덮어쓰기 때문입니다. 서버가 보관하는 것은 업로드된 원본과 그 파싱 결과뿐입니다.

### API

| | |
|---|---|
| `POST /api/files` | 업로드 → 프로파일 초안 + 첫 미리보기까지 한 번에 |
| `POST /api/sample` | 예시 장부 등록 (첫 화면용) |
| `POST /api/preview` | 프로파일을 실어 보내 미리보기 재계산 |
| `POST /api/convert` | 전체 행 변환 후 파일 다운로드 |
| `POST /api/json-schema` | 프로파일 → JSON Schema |
| `GET`/`POST /api/profiles` | 프로파일 저장·목록·조회 |

## 설치

```bash
uv venv
uv pip install -e ".[web]"     # 대시보드까지
uv pip install -e ".[dev]"     # 테스트 포함
```

## CLI

### 파일 구조 확인

```bash
everyfile inspect 일반전표_2026Q1.xlsx
```

```
[일반전표]
  헤더 행 : 3
  컬럼    : 전표일자, 계정코드, 계정과목, 적요, 거래처, 차변, 대변
  데이터  : 11행 / 전체 15행
  추론 타입:
    전표일자           → entryDate        date
    계정코드           → accountCode      code
    차변             → debit            money
  · 2행에 단위 표기 '원' 이 있습니다 — 금액은 자동 환산하지 않습니다
  · 헤더를 3행에서 찾았습니다 (위 2행은 머리글)
  · 소계·합계로 보이는 1개 행을 데이터에서 제외했습니다
```

### 변환

```bash
everyfile convert 일반전표.xlsx out.json          # 프로파일 자동 추론
everyfile convert 일반전표.xlsx out.csv --encoding cp949
everyfile convert 일반전표.xlsx out.xlsx --profile 전표표준.json --fail-on-error
```

### 프로파일 (컬럼 매핑 + 타입)

스키마를 손으로 쓰게 하면 아무도 쓰지 않으므로, 추론된 초안을 저장해 고쳐 쓰는 방식입니다.

```bash
everyfile profile 일반전표.xlsx -o 전표표준.json
everyfile profile 일반전표.xlsx -o schema.json --json-schema   # 표준 JSON Schema
```

프로파일은 스키마 버전을 가지므로, 저장해 둔 프로파일이 엔진 업데이트로 조용히
깨지지 않습니다.

### 미리보기 페이로드

대시보드가 렌더링할 JSON 계약입니다. 좌우 패널이 `sourceRow` 로 짝지어져 있고,
출력에서 빠진 행도 `included: false` 로 자리를 지킵니다.

```bash
everyfile preview 일반전표.xlsx -o preview.json
```

샘플링은 *앞 20행 + 이슈 행 + 결정적 무작위 20행* 입니다. 전체를 매번 변환하면 타입을
바꿀 때마다 화면이 멈추고, 앞부분만 보면 3만 번째 행에서 깨지는 파일을 놓칩니다.
시드가 고정되어 있어 같은 파일의 미리보기는 항상 동일합니다.

### 라이브러리로

```python
from everyfile import load

job = load("일반전표.xlsx")
job.profile.field_by_key("accountCode").type      # FieldType.CODE
job.result.records                                 # [{...}, ...]
job.result.issues                                  # [(행번호, 필드, Issue), ...]
job.save("out.json")
job.preview()                                      # 화면 페이로드
```

## 현재 지원 범위

| | 읽기 | 쓰기 |
|---|---|---|
| xlsx / xlsm | ✓ | ✓ |
| csv / tsv | ✓ (UTF-8 / CP949 / EUC-KR 자동 감지) | ✓ |
| json | ✓ (중첩 평탄화) | ✓ |
| pdf | — | — |
| docx / md | — | — |

## 안전장치

- **CSV 수식 인젝션 방어**: `=`, `+`, `-`, `@` 로 시작하는 값을 이스케이프합니다.
  외부에서 받은 파일을 변환해 다시 배포하는 일이 잦은 환경에서 필수입니다.
  음수 금액(`-1240000`)은 통과시킵니다.
- **CSV 출력 기본 인코딩 `utf-8-sig`**: BOM 이 없으면 한국어 윈도우 엑셀이 UTF-8 CSV 를
  CP949 로 읽어 한글이 전부 깨집니다. 구형 시스템용으로는 `--encoding cp949`.
- **엑셀 출력의 `검수필요` 시트**: 변환 산출물만 받아본 사람도 무엇을 확인해야 하는지
  파일 안에서 알 수 있습니다.

## 배포

`main` 에 푸시하면 `.github/workflows/deploy.yml` 이 테스트 → 사이트 조립 → Pages 배포를
수행합니다. 테스트가 깨지면 배포되지 않습니다.

사이트에는 엔진 휠(`everyfile`, `openpyxl`, `et_xmlfile`)이 함께 들어갑니다. 실행 시점에
PyPI 를 부르지 않는 이유는 사내망에서 외부 접속이 막혀 있을 수 있고, 받아 오는 버전이
조용히 바뀌면 어제 되던 변환이 오늘 달라지기 때문입니다.

```bash
.venv/bin/python scripts/build_site.py   # site/ 에 조립
```

### 최초 1회 설정

GitHub 저장소 **Settings → Pages → Source** 를 `GitHub Actions` 로 바꿔야 합니다.
기본값인 `Deploy from a branch` 는 파일을 그대로 올리기만 하므로 엔진 휠이 빠집니다.

### 브라우저 실행에서 외부로 나가는 것

- Pyodide 런타임 (`cdn.jsdelivr.net`) — 최초 1회, 이후 브라우저 캐시
- 웹폰트 (`fonts.googleapis.com`) — 없어도 동작하도록 비차단 로드

**변환 대상 파일은 나가지 않습니다.** 사내망이 위 두 주소를 막는다면 Pyodide 는
사이트에 함께 넣을 수도 있고, 그 경우 완전히 폐쇄망에서 돌릴 수 있습니다.

## 개발

```bash
.venv/bin/python -m pytest -q          # 120 tests
.venv/bin/python -m ruff check src tests
```

## 알아둘 제약

- **업로드 저장소는 프로세스 메모리 + 임시 디렉터리**입니다. 서버를 재시작하면 사라지고
  여러 워커로 띄우면 공유되지 않습니다. 사내 단일 인스턴스 용도의 MVP입니다.
- **미리보기는 전체 행을 변환한 뒤 표본만 내려보냅니다.** 이슈가 있는 행을 표본에
  넣으려면 전부 변환해 봐야 알 수 있기 때문입니다.
  덕분에 6,304행에 있는 깨진 값이 앞 20행만 보는 미리보기에서도 잡힙니다.
- **브라우저 실행의 실질 상한은 3만 행 근처**입니다. 그보다 큰 장부는 화면이 안내하며,
  CLI 나 사내 서버 쪽이 낫습니다. 실측은 `docs/브라우저-전환-검증.md` 에 있습니다.
- 프로파일은 임시 디렉터리에 저장되므로 아직 영구 저장소가 아닙니다.

## 다음 단계

1. PDF 표 추출 (pdfplumber, MIT — PyMuPDF/iText 는 AGPL 이라 사내 배포에 부적합)
   `SourceRef` 에 `page`/`bbox`/`confidence` 자리를 이미 비워 두었습니다
2. Markdown ↔ Word (문서 IR 추가)
3. 프로파일 영구 저장소와 고객사별 격리
4. 배치 처리 — 다중 파일, ZIP, 병합/분할, 파일명 규칙
5. 감사 로그
